version: '3.8'

services:
  invoice-redis:
    image: redis:7-alpine
    container_name: swift_sync_queue_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: always

volumes:
  redis_data: